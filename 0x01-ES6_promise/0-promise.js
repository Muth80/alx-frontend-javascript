function getResponseFromAPI() {
  return new Promise((resolve, reject) => {
    // Your api call or any code here;
    // If success
    resolve();

    // If error
    reject();
  });  
}

export default getResponseFromAPI;
